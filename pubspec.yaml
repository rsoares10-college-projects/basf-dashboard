name: basf_dashboard
description: BASF challenge project.

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_localizations:
    sdk: flutter

  dartz: ^0.10.0
  mockito: ^5.0.10
  dio: ^4.0.0
  flutter_modular: ^3.2.2+1
  flutter_mobx: ^2.0.0
  mobx: ^2.0.1
  mobx_codegen: ^2.0.1+3
  intl: ^0.17.0
  font_awesome_flutter: ^9.1.0
  fl_chart: ^0.40.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  effective_dart: ^1.3.0
  build_runner: ^2.0.4

flutter:
  uses-material-design: true
