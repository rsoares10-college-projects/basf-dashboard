import 'package:flutter/material.dart';

class DieselView extends StatefulWidget {
  const DieselView({Key? key}) : super(key: key);

  @override
  _DieselViewState createState() => _DieselViewState();
}

class _DieselViewState extends State<DieselView> {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('[Diesel View]'),
    );
  }
}
